// Console log only key-code
window.addEventListener('keydown', ({keyCode}) => {
     console.log(keyCode)
 })


// Console log full key detail
window.addEventListener('keydown', (event) =>{
     console.log(event)
})